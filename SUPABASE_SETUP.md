# إعداد Supabase وتشغيل SQL

تم إلغاء Prisma من التطبيق والاعتماد على PostgreSQL في Supabase مباشرة عبر مكتبة `pg` واستعلامات SQL مؤمّنة بالمعاملات والـ parameters.

## قبل النشر

1. خذ نسخة احتياطية من قاعدة البيانات.
2. افتح **Supabase Dashboard → SQL Editor**.
3. إذا كانت الترحيلات السابقة مطبقة، نفّذ فقط:
   - `supabase/APPLY_LATEST_FEATURES.sql`
4. إذا لم تطبق أي ترحيل سابق، نفّذ الملفات داخل `supabase/migrations` بالترتيب من `0001` إلى `0004`، أو استخدم `supabase/APPLY_ALL_MIGRATIONS.sql` بعد مراجعة النسخة الاحتياطية.
5. اضبط `DATABASE_URL` لمجمّع الاتصالات و`DIRECT_URL` للاتصال المباشر كما في `.env.example`.
6. شغّل `npm install` ثم `npm run build`.

## أوامر مفيدة

```bash
npm run db:migrate:latest
npm run db:seed
npm run build
```

> ملف `.env` الحقيقي غير موجود في الحزمة لحماية كلمات المرور. انسخ `.env.example` إلى `.env` وأضف بياناتك محلياً أو داخل Vercel.

## ما يضيفه الترحيل الأخير

- فعاليات وإعلانات مؤقتة تختفي من القوائم العامة بعد انتهائها.
- أنواع غرف الفنادق وأسعار مستقلة لكل نوع.
- حفظ نوع وسعر الغرفة داخل الحجز والتذكرة.
- فهرس لتسريع فلترة الفعاليات النشطة.
